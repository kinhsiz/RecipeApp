{
  "name": "recipeapp",
    "version": "3.0.0",
    "main": "",
    "scripts": {
        "start": "npm start --prefix backend",
        "install-client": "cd frontend && npm install && npm run build && cd ..",
        "install-server": "cd backend && npm install && cd .. ",
        "heroku-postbuild": "npm run install-client && npm run install-server"
    },
  "devDependencies": {
    "vite": "^5.1.5"
  },
  "dependencies": {
    "nodemon": "^3.1.0"
  }
}
