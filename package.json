{
  "name": "rollcall-rooms",
    "version": "1.0.0",
    "main": "",
    "scripts": {
        "start": "npm start --prefix server",
        "install-client": "cd backend && npm install && npm run build && cd ..",
        "install-server": "cd frontend && npm install && cd .. ",
        "heroku-postbuild": "npm run install-backend && npm run install-frontend"
    },
  "devDependencies": {
    "vite": "^5.1.5"
  },
  "dependencies": {
    "nodemon": "^3.1.0"
  }
}
